import "./GrainOverlay.css";

/** Fixed, cheap, whole-page film-grain layer — the one texture that ties every section together. */
export function GrainOverlay() {
  return <div className="grain-overlay" aria-hidden="true" />;
}
